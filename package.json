{
  "name": "plin-whatsapp-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker — API WhatsApp Photo",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
